import React from "react";

const {{name}} = (WrappedComponent) => {
    const {{name}}HOC = ({...otherProps}) => {
        return <WrappedComponent {...otherProps}/>
    };

    return {{name}}HOC
};

export default {{name}};